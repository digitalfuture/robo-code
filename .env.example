# Robot Connection Settings
# Robot acts as TCP Server, Vision system (this app) acts as TCP Client
VITE_ROBOT_IP=192.168.1.100
VITE_ROBOT_PORT=5000

# WebSocket Proxy Server URL
VITE_PROXY_URL=ws://localhost:3000

# Proxy Server Port
VITE_PROXY_PORT=3000

# Protocol: TCP String Protocol (ER Series Robot RCS2 V1.5.3)
# - Commands format: [Command();id=X]
# - Response format: [id = X; Ok; data] or [id = X; FAIL]
# - Default port: 5000 (confirmed by nmap scan)
