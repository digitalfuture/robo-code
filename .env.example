# Robot Connection Settings
# ==========================
# Copy this file to .env and configure for your robot

# Robot IP address
VITE_ROBOT_IP=192.168.1.100

# Robot Port - Choose ONE:
#   1502 = Modbus TCP (read coordinates/joints) - RECOMMENDED
#   5000 = TCP String Protocol (ER Series RCS2 V1.5.3 commands)
#   502  = Standard Modbus (limited data, not recommended)
# TIP: Can be switched in UI without restart after first connect!
VITE_ROBOT_PORT=1502

# WebSocket Proxy Server
VITE_PROXY_URL=ws://localhost:3000
VITE_PROXY_PORT=3000
