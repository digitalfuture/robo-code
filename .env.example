# Robot Connection Settings
# ==========================
# Copy this file to .env and configure for your robot

# Robot IP address
VITE_ROBOT_IP=192.168.1.100

# Robot Port - Choose ONE:
#   1502 = Modbus TCP (read coordinates/joints) - DEFAULT
#   5000 = TCP String Protocol (ER Series RCS2 V1.5.3 commands)
#   502  = Standard Modbus (limited data)
# TIP: Can be switched in UI without restart!
VITE_ROBOT_PORT=1502

# WebSocket Proxy Server
VITE_PROXY_URL=ws://localhost:3000
VITE_PROXY_PORT=3000

# Notes:
# - Modbus TCP (1502): Read-only for coordinates/joints
# - TCP String (5000): Full command control (GetCurJPos, MovePointV3, etc.)
# - Switch protocols in UI using toggle button (no restart needed)
# - Port from .env is used on first connect; UI changes save to localStorage
